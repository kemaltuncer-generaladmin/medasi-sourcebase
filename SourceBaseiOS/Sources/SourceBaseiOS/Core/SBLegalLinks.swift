import Foundation

/// Single source of truth for legal page URLs.
enum SBLegalLinks {
    static let privacyURL = URL(string: "https://sourcebase.medasi.com.tr/gizlilik")!
    static let termsURL = URL(string: "https://sourcebase.medasi.com.tr/kullanim-kosullari")!
}
