# TODO — Repository Indexer (SourceBase)

> Generated by Repository Indexer agent. Working dir: `/Users/kemaltuncer/Desktop/swiftsourcebase`
> Scope: index the repo; emit a proposed `PROJECT_INDEX.md` / `PROJECT_INDEX.json` as file blocks **inside this TODO only**. No other files are created.

---

## Implementation Status

**Completed on 2026-06-03.** The proposed index blocks are now real repository files:

- `PROJECT_INDEX.md`
- `PROJECT_INDEX.json`

The index omits credential values, keeps the Supabase anon key redacted, and was validated alongside package/app builds.

---

## Context

SourceBase is an iOS/SwiftUI study app backed by Supabase. It is organized as one Xcode app shell plus two local Swift packages.

- `App/` — SwiftUI Xcode project (`SourceBase.xcodeproj`). Thin executable shell; `@main` lives here.
- `SourceBaseiOS/` — Swift Package, the UI + app layer (DesignSystem, Core stores, Features).
- `SourceBaseBackend/` — Swift Package, the data/service layer (Supabase client, Auth, Drive, Profile, Config).
- `docs/` — design system, launch plan, agent guardrails, backend contract.

**Repo facts (verified):** 92 `.swift` files; ~23,578 LOC across source dirs; Swift tools 6.0; platforms iOS 17 / macOS 14.

> **git history is UNAVAILABLE** — this is **not** a git repository (no `.git`). Change-hotspot detection below uses **file modification times and file sizes** as proxies, not commit frequency. Treat hotspots as heuristic.

> **Secrets note:** `SourceBaseConfig.Defaults` contains a baked Supabase **anon** key (a public client token, by design). It is intentionally **excluded** from all index output below; no keys/credentials are reproduced.

---

## Indexing Plan

- [x] **RI-PLAN-1.1** Map directory structure across focus areas (source / tests / config / docs / scripts).
- [x] **RI-PLAN-1.2** Identify entry points (executable `@main`, root view, bootstrap).
- [x] **RI-PLAN-1.3** Identify service boundaries & module interfaces (package products, public types).
- [x] **RI-PLAN-1.4** Graph dependency relationships (internal package edges + external SwiftPM deps).
- [x] **RI-PLAN-1.5** Detect change hotspots via mtime + size (git unavailable — stated explicitly).
- [x] **RI-PLAN-1.6** Produce compressed, token-efficient index summary (<~2000 tokens).
- [x] **RI-PLAN-1.7** Emit proposed `PROJECT_INDEX.md` and `PROJECT_INDEX.json` as labeled file blocks.
- [x] **RI-PLAN-1.8** Verify every referenced path exists; redact secrets.

---

## Indexing Items

### Directory map (hierarchical)

- [x] **RI-ITEM-1.1** `App/SourceBase/` — `SourceBaseAppMain.swift` (@main, 12 LOC), `Info.plist`, `Assets.xcassets/` (AppIcon, LaunchBackground). Build artifacts in `App/build/SourceBase.xcarchive/` (ignore for indexing).
- [x] **RI-ITEM-1.2** `SourceBaseBackend/Sources/SourceBaseBackend/` — `Config/`, `Auth/`, `Drive/`, `Profile/`. Tests in `Tests/SourceBaseBackendTests/` (`AuthTests`, `DriveTests`).
- [x] **RI-ITEM-1.3** `SourceBaseiOS/Sources/SourceBaseiOS/` — `App/`, `Core/`, `DesignSystem/` (24 SB* components), `Features/` (Auth, BaseForce, CentralAI, Drive, Profile, SourceLab, Study). Tests in `Tests/SourceBaseiOSTests/` (`UploadAndOutputTests`).
- [x] **RI-ITEM-1.4** `docs/` — 13 markdown files (AGENTS, APP_STORE_LAUNCH_PLAN, DESIGN_SYSTEM, DRIVE_BACKEND_CONTRACT, PATCH_PROTOCOL, etc.). No `scripts/` directory exists.

### Entry points

- [x] **RI-ITEM-2.1** Executable `@main`: `App/SourceBase/SourceBaseAppMain.swift` → embeds `SourceBaseRootView()`.
- [x] **RI-ITEM-2.2** Root view + bootstrap: `SourceBaseiOS/.../App/SourceBaseApp.swift` (`SourceBaseRootView`, `RootView`, `MainNavigationView`). Wires `AppState`, StoreKit listener, deep-link `onOpenURL`.
- [x] **RI-ITEM-2.3** App bootstrap/state machine: `Core/AppState.swift` (`AppState.shared`, `bootstrap()`, `initialRoute`, `handleOpenURL`, `signOut`). Routing via `Core/AppRouter.swift`; session via `Core/SessionStore.swift`; data via `Core/SourceBaseWorkspaceStore.swift` (largest Core file, 29 KB).
- [x] **RI-ITEM-2.4** Deep link scheme: `sourcebase://auth/callback` (handled in `AppState.handleOpenURL`).

### Service boundaries / module interfaces

- [x] **RI-ITEM-3.1** Package products: `SourceBaseBackend` (library) and `SourceBaseiOS` (library). App target depends on `SourceBaseiOS` only.
- [x] **RI-ITEM-3.2** Backend public surface (verified, 44 public types incl.): `SourceBaseConfig`, `AuthBackend` (actor), `AuthResult`/`AuthError`, `DriveAPI` + `DriveAPIError`, `DriveRepository`, `ProfileRepository`, `StoreRepository`, `DriveUploadService`, model structs (`DriveCourse`, `DriveSection`, `DriveFile`, `GeneratedOutput`, `GenerationJobSnapshot`, `SBFlashcard`, `SBQuestionPrompt`, `SBStudyTemplateContent`, etc.), `GeneratedContentParser`.
- [x] **RI-ITEM-3.3** Single backend RPC chokepoint: `DriveAPI.invoke(action:payload:)` → Supabase Edge Function `"sourcebase"`. All Drive/generation/store/IAP calls funnel through this action-dispatch envelope (`{ ok, error, ... }`).
- [x] **RI-ITEM-3.4** Auth singleton: `AuthBackend.shared` (actor) owns the `SupabaseClient`; `getClient()` gate used by `DriveAPI`, StoreKit redeem, etc.

### Dependency graph

- [x] **RI-ITEM-4.1** Internal edges: `App` → `SourceBaseiOS` → `SourceBaseBackend`. (`SourceBaseiOS/Package.swift` references `../SourceBaseBackend` by path.)
- [x] **RI-ITEM-4.2** External direct deps: Backend → `supabase-swift` (>=2.0.0, resolved 2.46.0). iOS → `SwiftUI-Shimmer` (1.5.1), `Pow` (1.0.6), `lottie-spm` (4.6.0).
- [x] **RI-ITEM-4.3** Transitive (from `Package.resolved`): swift-asn1 1.7.0, swift-clocks 1.0.6, swift-concurrency-extras 1.4.0, swift-crypto 4.5.0, swift-http-types 1.5.1, xctest-dynamic-overlay 1.9.0.

### Change hotspots (heuristic — git unavailable; mtime + size proxies)

- [x] **RI-ITEM-5.1** Largest source files (size ≈ complexity): `Features/Study/GeneratedOutputStudyView.swift` (34 KB), `Drive/DriveRepository.swift` (40 KB, backend), `Drive/GeneratedContentModels.swift` (24 KB), `BaseForce/ResultView.swift` (24 KB), `Core/SourceBaseWorkspaceStore.swift` (29 KB).
- [x] **RI-ITEM-5.2** Most recently modified cluster (Jun 3, 22:00–23:10) — active edit front: `SourceLab/ExamMorningView`, `SourceLab/ClinicalView`, `SourceLab/SourceLabToolFlowView`, `BaseForce/Summary/Comparison/Question/Algorithm/FlashcardFactoryView`, `BaseForce/ResultView`, `Study/GeneratedOutputStudyView`, `Study/SBStudyExportService`, `Drive/FileDetailView`.
- [x] **RI-ITEM-5.3** Recently touched backend (Jun 3 ~13:30): `DriveRepository`, `DriveModels`, `DriveAPI`, `GeneratedContentModels`, `ProfileRepository` + tests `DriveTests`, `UploadAndOutputTests`. Indicates Drive/generation pipeline is the current hotspot.
- [x] **RI-ITEM-5.4** Stable/low-churn (May 30–31, small): DesignSystem primitives (`SBSpacing`, `SBRadius`, `SBColors`, `SBTypography`), `UniversityCatalog`, `AuthModels`, `AuthErrors`.

### Tests & config

- [x] **RI-ITEM-6.1** Tests: `AuthTests.swift`, `DriveTests.swift` (backend); `UploadAndOutputTests.swift` (iOS). No UI/snapshot tests detected.
- [x] **RI-ITEM-6.2** Config: `App/SourceBase/Info.plist`; runtime config `SourceBaseConfig.fromEnvironment()` with baked `Defaults` (anon key redacted here) overridable via `SOURCEBASE_*` env vars.

---

## Implemented Code Changes

> These file contents were written to `PROJECT_INDEX.md` and `PROJECT_INDEX.json`; the embedded blocks remain as the original audit trail.

### Implemented file: `PROJECT_INDEX.md`

```markdown
# SourceBase — Project Index

iOS/SwiftUI study app on Supabase. Xcode app shell + 2 local Swift packages.
Swift 6.0 · iOS 17 / macOS 14 · ~92 swift files · ~23.6k LOC.
NOTE: not a git repo — hotspots are mtime/size heuristics.

## Layout
- App/SourceBase/           Xcode app shell (@main)
- SourceBaseiOS/            UI package (App, Core, DesignSystem, Features)
- SourceBaseBackend/        Service package (Config, Auth, Drive, Profile)
- docs/                     Design system, launch plan, contracts

## Entry points
- App/SourceBase/SourceBaseAppMain.swift  → @main, embeds SourceBaseRootView
- SourceBaseiOS/.../App/SourceBaseApp.swift  → RootView + nav + StoreKit + deep links
- SourceBaseiOS/.../Core/AppState.swift  → bootstrap(), initialRoute, handleOpenURL
- Deep link: sourcebase://auth/callback

## Modules / boundaries
- Products: SourceBaseiOS (lib), SourceBaseBackend (lib)
- AuthBackend.shared (actor)  → owns SupabaseClient
- DriveAPI.invoke(action:payload:)  → single Edge Function "sourcebase" RPC envelope
- Repositories: DriveRepository, ProfileRepository, StoreRepository
- Stores: AppState, SessionStore, AppRouter, SourceBaseWorkspaceStore
- Features: Auth, BaseForce, CentralAI, Drive, Profile, SourceLab, Study
- DesignSystem: 24 SB* components (SBColors, SBButton, SBCard, SBFileCard, ...)

## Dependency graph
App → SourceBaseiOS → SourceBaseBackend
SourceBaseBackend → supabase-swift (2.46.0)
SourceBaseiOS → SwiftUI-Shimmer 1.5.1, Pow 1.0.6, lottie-spm 4.6.0
Transitive: swift-crypto, swift-asn1, swift-http-types, swift-clocks,
            swift-concurrency-extras, xctest-dynamic-overlay

## Hotspots (mtime/size heuristic — no git)
- Active edits (Jun 3): SourceLab/* , BaseForce/*FactoryView, ResultView,
  Study/GeneratedOutputStudyView, Drive/FileDetailView
- Largest: DriveRepository(40KB), GeneratedOutputStudyView(34KB),
  SourceBaseWorkspaceStore(29KB), ResultView(24KB), GeneratedContentModels(24KB)
- Stable: DesignSystem primitives, AuthModels/Errors, UniversityCatalog

## Tests
- Backend: AuthTests, DriveTests
- iOS: UploadAndOutputTests

## Config
- App/SourceBase/Info.plist
- SourceBaseConfig.fromEnvironment() + baked Defaults (anon key — public client token)
- Env overrides: SOURCEBASE_SUPABASE_URL / _ANON_KEY / _PUBLIC_URL / _MOBILE_REDIRECT_URL
```

### Implemented file: `PROJECT_INDEX.json`

```json
{
  "project": "SourceBase",
  "type": "ios-swiftui-app",
  "vcs": "none (not a git repository; hotspots are mtime/size heuristics)",
  "toolchain": { "swiftTools": "6.0", "platforms": ["iOS 17", "macOS 14"] },
  "stats": { "swiftFiles": 92, "approxLOC": 23578 },
  "modules": [
    {
      "name": "App",
      "path": "App/SourceBase",
      "role": "xcode-app-shell",
      "entry": "App/SourceBase/SourceBaseAppMain.swift",
      "dependsOn": ["SourceBaseiOS"]
    },
    {
      "name": "SourceBaseiOS",
      "path": "SourceBaseiOS/Sources/SourceBaseiOS",
      "role": "ui-app-layer",
      "product": "library",
      "groups": ["App", "Core", "DesignSystem", "Features"],
      "features": ["Auth", "BaseForce", "CentralAI", "Drive", "Profile", "SourceLab", "Study"],
      "dependsOn": ["SourceBaseBackend", "SwiftUI-Shimmer", "Pow", "lottie-spm"]
    },
    {
      "name": "SourceBaseBackend",
      "path": "SourceBaseBackend/Sources/SourceBaseBackend",
      "role": "service-data-layer",
      "product": "library",
      "groups": ["Config", "Auth", "Drive", "Profile"],
      "dependsOn": ["supabase-swift"]
    }
  ],
  "entryPoints": {
    "main": "App/SourceBase/SourceBaseAppMain.swift",
    "rootView": "SourceBaseiOS/Sources/SourceBaseiOS/App/SourceBaseApp.swift",
    "bootstrap": "SourceBaseiOS/Sources/SourceBaseiOS/Core/AppState.swift",
    "deepLink": "sourcebase://auth/callback"
  },
  "serviceBoundaries": {
    "authActor": "AuthBackend.shared",
    "rpcChokepoint": "DriveAPI.invoke(action:payload:) -> EdgeFunction:sourcebase",
    "repositories": ["DriveRepository", "ProfileRepository", "StoreRepository"],
    "stores": ["AppState", "SessionStore", "AppRouter", "SourceBaseWorkspaceStore"]
  },
  "dependencies": {
    "internal": ["App->SourceBaseiOS", "SourceBaseiOS->SourceBaseBackend"],
    "external": {
      "supabase-swift": "2.46.0",
      "SwiftUI-Shimmer": "1.5.1",
      "Pow": "1.0.6",
      "lottie-spm": "4.6.0"
    },
    "transitive": ["swift-asn1 1.7.0", "swift-clocks 1.0.6", "swift-concurrency-extras 1.4.0", "swift-crypto 4.5.0", "swift-http-types 1.5.1", "xctest-dynamic-overlay 1.9.0"]
  },
  "hotspots": {
    "method": "file mtime + size (git unavailable)",
    "activeEdits": ["SourceLab/ExamMorningView.swift", "SourceLab/ClinicalView.swift", "SourceLab/SourceLabToolFlowView.swift", "BaseForce/SummaryFactoryView.swift", "BaseForce/ComparisonFactoryView.swift", "BaseForce/QuestionFactoryView.swift", "BaseForce/AlgorithmFactoryView.swift", "BaseForce/FlashcardFactoryView.swift", "BaseForce/ResultView.swift", "Study/GeneratedOutputStudyView.swift", "Drive/FileDetailView.swift"],
    "largest": ["Drive/DriveRepository.swift", "Study/GeneratedOutputStudyView.swift", "Core/SourceBaseWorkspaceStore.swift", "BaseForce/ResultView.swift", "Drive/GeneratedContentModels.swift"],
    "stable": ["DesignSystem/SBSpacing.swift", "DesignSystem/SBColors.swift", "Auth/AuthModels.swift", "Auth/UniversityCatalog.swift"]
  },
  "tests": {
    "backend": ["Tests/SourceBaseBackendTests/AuthTests.swift", "Tests/SourceBaseBackendTests/DriveTests.swift"],
    "ios": ["Tests/SourceBaseiOSTests/UploadAndOutputTests.swift"]
  },
  "config": {
    "infoPlist": "App/SourceBase/Info.plist",
    "runtime": "SourceBaseConfig.fromEnvironment()",
    "envOverrides": ["SOURCEBASE_SUPABASE_URL", "SOURCEBASE_SUPABASE_ANON_KEY", "SOURCEBASE_PUBLIC_URL", "SOURCEBASE_MOBILE_REDIRECT_URL"],
    "secretsRedacted": true
  }
}
```

---

## Commands

```bash
# Re-scan directory tree (excludes build artifacts)
find . -type d -not -path '*/.build/*' -not -path '*/build/*' -not -path '*/.swiftpm/*'

# Swift file count + LOC
find . -name '*.swift' -not -path '*/.build/*' -not -path '*/build/*' | wc -l
find ./App/SourceBase ./SourceBaseBackend/Sources ./SourceBaseiOS/Sources -name '*.swift' | xargs wc -l | tail -1

# Hotspot proxy (no git): recent edits + largest files
find ./SourceBaseiOS/Sources ./SourceBaseBackend/Sources -name '*.swift' -exec ls -la {} \; | sort -k6,8
find ./SourceBaseiOS/Sources ./SourceBaseBackend/Sources -name '*.swift' -exec ls -S {} +

# Public API surface (backend)
grep -rhoE 'public (struct|class|enum|actor|protocol) [A-Za-z0-9_]+' SourceBaseBackend/Sources | sort -u

# Resolved dependency versions
cat App/SourceBase.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# Build (packages)
swift build --package-path SourceBaseBackend
swift build --package-path SourceBaseiOS

# Test
swift test --package-path SourceBaseBackend
swift test --package-path SourceBaseiOS
```

---

## Index Quality Task Checklist

- [x] **RI-QA-1** All focus areas scanned (source, tests, config, docs); confirmed no `scripts/` dir.
- [x] **RI-QA-2** Entry points verified against actual files (`SourceBaseAppMain`, `SourceBaseApp`, `AppState`).
- [x] **RI-QA-3** Service boundaries map to verified public types (44 public symbols enumerated in backend).
- [x] **RI-QA-4** Internal dependency edges verified from both `Package.swift` files.
- [x] **RI-QA-5** External dependency versions cross-checked against `Package.resolved`.
- [x] **RI-QA-6** Hotspot method explicitly stated as mtime/size (git history unavailable).
- [x] **RI-QA-7** Compressed summary kept under ~2000 tokens (PROJECT_INDEX.md block).
- [x] **RI-QA-8** `PROJECT_INDEX.md` and `PROJECT_INDEX.json` written to disk from the proposed labeled blocks.
- [x] **RI-QA-9** All referenced paths verified to exist.
- [x] **RI-QA-10** No secrets/API keys/credentials reproduced (baked anon key redacted).
- [x] **RI-QA-11** Repository index artifacts intentionally written to disk; TODO retained as audit trail.
